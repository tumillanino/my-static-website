{
  "name": "anthony-tumia-site",
  "private": true,
  "scripts": {
    "build": "npm run sync:blog && hugo --cleanDestinationDir --baseURL https://anthonytumia.com/",
    "sync:blog": "node scripts/sync-r2-blog.mjs",
    "serve": "hugo server"
  },
  "devDependencies": {
    "hugo-bin": "0.146.1"
  },
  "hugo-bin": {
    "buildTags": "extended"
  }
}
